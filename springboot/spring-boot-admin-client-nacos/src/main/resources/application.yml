server:
  port: 9001
  servlet:
    context-path: /admin-client-nacos
spring:
  application:
    name: admin-client-nacos
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  file:
    name: admin-client.log
